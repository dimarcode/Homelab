name: tsdproxy
services:
  tsdproxy:
    image: almeidapaulopt/tsdproxy:1
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /mnt/nvme-thin/docker/tsdproxy/data:/data
      - /mnt/nvme-thin/docker/tsdproxy/config:/config
    environment:
      - TSDPROXY_AUTHKEY=${TSDPROXY_TS_AUTHKEY}
      - DOCKER_HOST=unix:///var/run/docker.sock
    restart: unless-stopped
    ports:
      - "8080:8080"