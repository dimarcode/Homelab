---
- name: Install-docker-test
  hosts: testing
  become: true
  roles:
    - geerlingguy.docker
  vars_files:
    - group_vars/secrets.yml
  vars:
    ansible_become_pass: "{{ ansible_become_pass_id }}"
   

